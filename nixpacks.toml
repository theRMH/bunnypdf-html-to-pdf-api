[phases.setup]
nixPkgs = [
  "alsa-lib",
  "at-spi2-atk",
  "atk",
  "cairo",
  "cups",
  "gtk3",
  "libdrm",
  "libx11",
  "libxcomposite",
  "libxdamage",
  "libxfixes",
  "libxkbcommon",
  "libxrandr",
  "mesa",
  "nspr",
  "nss",
  "pango"
]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npx playwright install chromium"]

[start]
cmd = "npm start"
